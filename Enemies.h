/*
 * Enemies.h
 *
 *  Created on: 10 Dec 2017
 *      Author: apl
 */

/*
#ifndef ENEMIES_H_
#define ENEMIES_H_

#include <vector>
#include <ncurses.h>

class Enemies {
public:
	Enemies(WINDOW * win, int& score);
	void addEnemy();
	void kill(int& score ,std::pair<int,int>);
	void addrow(int row);
	void display();

private:
	int xLoc, yLoc, xMax, yMax;
	char character;
	WINDOW * curwin;
};

#endif /* ENEMIES_H_ */
*/
